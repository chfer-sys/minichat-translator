services:
  minichat-translator:
    image: nginx:alpine
    container_name: minichat-translator
    restart: unless-stopped
    ports:
      - "8081:80"
    volumes:
      - .:/usr/share/nginx/html:ro
    environment:
      - TZ=America/New_York